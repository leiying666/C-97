#include<stdio.h>

int main()
{
    int* pa = (int*)malloc(1000 * sizeof(int));
    if (pa == NULL)
    {
        return 0;
    }
    int n, m;
    scanf("%d", &n);
    for (int i = 0; i < n; i++)
    {
        scanf("%d", &pa[i]);
    }
    scanf("%d", &m);
    if (m == n)
    {
        int i;
        for (i = 0; i < n; i++)
        {
            printf("%d ", pa[i]);
        }
    }
    else if (m > n)
    {
        int tmp=m%n;
        if (tmp == 0)
        {
            int i;
            for (i = 0; i < n; i++)
            {
                printf("%d ", pa[i]);
            }
        }
        else
        {
            for (int i = 0; i < n; i++)
            {
                if (i - m >= 0)
                {
                    printf("%d ", pa[i - m]);
                }
                else
                {
                    int tmp = n-m + i;
                    {
                        printf("%d ", pa[tmp]);
                    }
                }
            }
        }
    }
    else
    {
        for (int i = 0; i < n; i++)
        {
            if (i - m >= 0)
            {
                printf("%d ", pa[i - m]);
            }
            else
            {
                int tmp = n - m + i;
                {
                    printf("%d ", pa[tmp]);
                }
            }
        }
    }
    free(pa);
    pa = NULL;
    return 0;
}
